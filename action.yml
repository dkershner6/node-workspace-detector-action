# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: Node / NPM Workspace Detector Action
description: Detect the parent-most directory that contains a Node or NPM Project, and return the result in outputs.
runs:
  using: node20
  main: dist/index.js
outputs:
  workspace:
    description: The parent-most path where a package.json exists
branding:
  icon: search
  color: purple
